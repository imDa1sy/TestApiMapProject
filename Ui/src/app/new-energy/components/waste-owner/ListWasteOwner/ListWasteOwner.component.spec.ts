import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { ListWasteOwnerComponent } from './ListWasteOwner.component';


/*

describe('ListWasteOwnerComponent', () => {
  let component: ListWasteOwnerComponent;
  let fixture: ComponentFixture<ListWasteOwnerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListWasteOwnerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListWasteOwnerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/